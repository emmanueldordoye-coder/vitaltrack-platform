{
  "name": "vitaltrack-platform",
  "version": "0.1.0",
  "description": "VitalTrack Platform - Health monitoring and tracking system",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c magenta,cyan \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "start": "npm start --workspace=backend",
    "test": "npm test --workspace=frontend && npm test --workspace=backend",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=backend",
    "format": "npm run format --workspace=frontend && npm run format --workspace=backend",
    "type-check": "npm run type-check --workspace=frontend && npm run type-check --workspace=backend",
    "ci:frontend": "npm run lint --workspace=frontend && npm run type-check --workspace=frontend && npm run test --workspace=frontend --if-present",
    "ci:backend": "npm run lint --workspace=backend && npm run type-check --workspace=backend && npm run test --workspace=backend --if-present",
    "ci": "npm run ci:frontend && npm run ci:backend"
  },
  "keywords": [
    "health",
    "monitoring",
    "tracking"
  ],
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
